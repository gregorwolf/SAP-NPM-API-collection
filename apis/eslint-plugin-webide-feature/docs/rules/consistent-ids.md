# Each "package.json" and "plugin.json" file must have consistent IDs. (_consistent-ids_)

## Rule Details

In every **package.json** and **plugin.json** file there must be consistent IDs, or the _consistent-ids_ rule displays an alert.
